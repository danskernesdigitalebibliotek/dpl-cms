<div class="mt-64" data-related-result-basis="{{ result_basis|join(', ') }}">
  {% block content %}
    {{ title }} | {{ link }}

    {% for item in items %}
      {{ item }}
    {% endfor %}
  {% endblock content %}
</div>
