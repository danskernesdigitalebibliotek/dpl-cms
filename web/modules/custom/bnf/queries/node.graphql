query GetNode($id: ID!) {
  info {
    name
    url
  }
  node(id: $id) {
    ... on NodeInterface {
      id,
      title,
      path
    }
    ... on NodeArticle {
      canonicalUrl {
        title
        url
      }
      paragraphs {
        ... on ParagraphTextBody {
          body {
            value
            format
          }
        }
        ... on ParagraphAccordion {
          accordionDescription {
            value
            format
          }
          accordionTitle {
            value
            format
          }
        }
      }
    }
  }
}
