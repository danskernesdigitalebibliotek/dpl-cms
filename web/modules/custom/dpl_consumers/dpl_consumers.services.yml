---
services:
  dpl_consumers.consumer_handler:
    class: Drupal\dpl_consumers\Services\ConsumerHandler
    autowire: true
    arguments:
      $logger: '@logger.channel.dpl_consumers'

  logger.channel.dpl_consumers:
    parent: logger.channel_base
    arguments:
      - 'dpl_consumers'
