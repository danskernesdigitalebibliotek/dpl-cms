{% if node.bundle != 'page' %}
  {% set date_tag = node.getCreatedTime()|format_date('date_full_month') %}
{% endif %}

{% include '@novel/components/card.html.twig'
  with {
  attributes: attributes,
  media: content.field_teaser_image,
  variant: 'x-large',
  has_media: (content.field_teaser_image.0),
  teaser_text: content.field_teaser_text,
  type_tag: node.type.entity.label(),
  date_tag: date_tag,
  title: label,
  url: url
} only %}
