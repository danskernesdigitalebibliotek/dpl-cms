{#
  An image can have a byline, which uses a <figcaption>.
  Therefore, it is important that we wrap it in a <figure>.
#}
<figure {{ attributes.addClass('media') }}>
  {{ title_suffix.contextual_links }}
  {{ content }}
</figure>
