<ul class="content-list">
  {% for row in rows %}
    <li class="content-list__item {{ row.shouldBeStacked ? ' content-list__item--stacked' }}">
      {{ row.content }}
    </li>
  {% endfor %}
</ul>
